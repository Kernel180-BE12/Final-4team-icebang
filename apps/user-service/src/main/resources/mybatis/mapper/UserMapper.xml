<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gltkorea.icebang.mapper.UserMapper">

    <!-- 기존 쿼리 (수정) -->
    <select id="findByEmail" parameterType="String" resultType="com.gltkorea.icebang.dto.UserAuthDto">
        SELECT
            user_id AS userId,
            name,
            email,
            password,
            provider,
            provider_id AS providerId,
            status
        FROM
            "USER"
        WHERE
            email = #{email}
    </select>

    <!-- 회원가입용 - 사용자 추가 -->
    <insert id="insertUser" parameterType="com.gltkorea.icebang.dto.UserAuthDto">
        INSERT INTO "USER" (user_id, name, email, password, provider, provider_id, status)
        VALUES (#{userId}, #{name}, #{email}, #{password}, #{provider}, #{providerId}, #{status})
    </insert>

    <!-- 로그인용 - 이메일과 비밀번호로 조회 -->
    <select id="findByEmailAndPassword" resultType="com.gltkorea.icebang.dto.UserAuthDto">
        SELECT
            user_id AS userId,
            name,
            email,
            password,
            provider,
            provider_id AS providerId,
            status
        FROM
            "USER"
        WHERE
            email = #{email} AND password = #{password}
    </select>

    <!-- ID로 사용자 조회 -->
    <select id="findByUserId" parameterType="String" resultType="com.gltkorea.icebang.dto.UserAuthDto">
        SELECT
            user_id AS userId,
            name,
            email,
            password,
            provider,
            provider_id AS providerId,
            status
        FROM
            "USER"
        WHERE
            user_id = #{userId}
    </select>

</mapper>