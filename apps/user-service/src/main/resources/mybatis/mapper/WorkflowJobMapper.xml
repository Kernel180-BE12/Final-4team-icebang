<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.icebang.domain.mapping.mapper.WorkflowJobMapper">

    <select id="findJobIdsByWorkflowId" resultType="long">
        SELECT
            job_id
        FROM
            workflow_job
        WHERE
            workflow_id = #{workflowId}
        ORDER BY
            execution_order ASC
    </select>

</mapper>