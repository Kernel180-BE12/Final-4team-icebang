spring:
  application:
    name: mvp
  profiles:
    active: develop
  test:
    context:
      cache:
        maxSize: 1
mybatis:
  # Mapper XML 파일 위치
  mapper-locations: classpath:mapper/**/*.xml
  type-handlers-package: site.icebang.config.mybatis.typehandler