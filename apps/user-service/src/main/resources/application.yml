spring:
  application:
    name: mvp
  profiles:
    active: develop
mybatis:
  # Mapper XML 파일 위치
  mapper-locations: classpath:mapper/**/*.xml
  type-handlers-package: com.gltkorea.icebang.config.mybatis.typehandler